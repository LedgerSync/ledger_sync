# Gemfile

Add this line to your application’s Gemfile:

```shell
gem 'ledger_sync'
```

And then execute:

```shell
bundle
```

---

# Directly

Or install it yourself as:

```
gem install ledger_sync
```